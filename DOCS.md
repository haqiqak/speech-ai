# DOCS.md — File map

One line per file/module: what it's for, who reads it, how often it
should change, and — per Practice.md §5/§7 — an explicit flag where a
doc is already known to have drifted from the code it describes.

**Scope note (Stage 2, this pass):** the repository was narrowed to the text
reformulation module. `voice.py`, `profiling/asr.py`, and `profiling/detect.py`
moved to `out_of_scope/` (audio/ASR/voice-UI code that belongs to the separate
Audio Module, not this repo) — see `out_of_scope/README.md`. This was a pure
move/removal-from-the-live-path pass: no reformulation algorithm, threshold,
weight, or scoring method was changed. See `DECISION_LOG.md` for the entry
recording this decision.

**Architecture freeze notice (2026-08-28):** the reformulation pipeline
(`reformulate.py`/`rephrase.py`/`semantic.py`) is frozen as the
maintained/shipped baseline, ratified after a formal Architecture
Go/No-Go assessment — see `CLAUDE.md`'s freeze banner and
`VALIDATION.md` §56 before proposing any change to those files.

## Documentation (reader: humans, new contributors, future Claude instances)

| File | Purpose | Update cadence | Drift status |
|---|---|---|---|
| `README.md` | Explains the overall Speech-AI project, where this repo fits, and this module's product description, setup, feature list, "Architecture Notes" | On every user-visible feature/behavior change | **Drift fixed, this pass**: previously described the synonym-ranking split as `0.65 sim / 0.35 freq`; now matches `semantic.py`'s actual constants (`SEMANTIC_W=0.90`, `FREQUENCY_W=0.10`, `MIN_SEMANTIC=0.85`). Also rewritten to state project-vs-module scope explicitly and to drop the voice/mic feature descriptions (moved to `out_of_scope/`). |
| ~~`AUTH_README.md`~~ | *Removed 2026-08-16* — documented `auth.py`/`user_store.py`, both deleted in the Stage 4A refinement (no more login/multi-user system; see `DECISION_LOG.md` 2026-08-16-A). Kept describing non-existent code would actively mislead a reader rather than just being stale, so it was removed rather than left with a drift warning — recoverable from git history if ever needed. Its still-useful content (why SHA-256 was chosen, the known credential-exposure issue) already lives in `DECISION_LOG.md` 2026-06-07-A / 2026-06-13-A / 2026-08-16-A. | — | — |
| `scripts/README.md` | How to use `build_rephrase_dataset.py` / `train_rephrase.py` | On scripts changes | Not reviewed in this pass. |
| ~~`changes.md`~~ | *Removed 2026-08-19* — pre-`CHANGELOG.md` narrative release log (last entry v6.0.1, 2026-06-13). Fully superseded in function by `CHANGELOG.md` (disciplined, cross-referenced into `DECISION_LOG.md`, kept current through R27) and described features that no longer exist in this repo (microphone profiling, the CrisperWhisper toggle, a `run_app.ps1` launcher already removed) — kept it would have actively misled a reader, same reasoning as `AUTH_README.md`'s removal above. Recoverable from git history. | — | — |
| `Practice.md` (external, uploaded) | Governing research/engineering methodology for this module | Amended per its own §20 (dated addenda only) | N/A — this is the standard everything else is checked against. |
| `CLAUDE.md` | Orientation, points elsewhere | Rarely | New, this pass. |
| `HANDOFF.md` | Curated onboarding, proven-vs-hypothesis, pitfalls | Every session that learns something a future session needs | New, this pass — currently a one-time backfill, not yet a live log. |
| `DECISION_LOG.md` | Append-only, four-part decision record | Every non-trivial decision, at the time it's made | New, this pass — backfilled from git history/comments (see caveat in `CLAUDE.md`). Going forward this should be appended to live, not regenerated. |
| `VALIDATION.md` | Pre-registered protocols, results, ablations, named limitations | Every real evaluation run | New, this pass — currently documents *what evaluation machinery exists*, not a completed pre-registered result, because none exists yet (see §12 proxy-metric warning inside the file). |
| `ROADMAP.md` | Priority-ordered, evidence-linked forward plan | As evidence changes priorities | New, this pass — seeded from README's existing "Roadmap" section plus gaps this review surfaced. |
| `CHANGELOG.md` | One line per change, reverse-chronological, points into decision log | Every commit that closes a decision-log entry | New, this pass — backfilled from `git log`. |
| `RESEARCH.md` | Stage 3 literature/technical-approach review (paraphrase generation, lexical substitution, simplification, semantic-preservation evaluation, controlled generation, phoneme-aware NLP, personalization) plus a component-by-component critical assessment of this repo's own implementation against that literature. Satisfies `ROADMAP.md` R3. | On the next research pass, or when new evidence changes the assessment | New (2026-08-15). Every claim tagged `[FINDING]`/`[INTERPRETATION]`/`[HYPOTHESIS]`/`[LIMITATION]`/`[FUTURE WORK]`/`[RECOMMENDATION]` per its own legend — read the legend before citing a specific claim from it. Explicitly a research pass, not an implementation change: nothing in the repo's behavior changed while producing it. |
| `PROBLEM_FORMULATION.md` | Stage 4A design record for the persistent speaker difficulty profile: problem formulation, schema, ARPAbet-vs-IPA and JSON-vs-SQLite research, the text-entry/flagging interaction design (and why a JS-based one was researched but deliberately not built), rejected alternatives, testing results, and a foundation audit (heteronym/round-trip ambiguities found and fixed). | When the profile schema or its interaction design changes | New (2026-08-15), extended 2026-08-16. Uses `RESEARCH.md`'s same finding/interpretation/hypothesis/limitation/recommendation legend. Read before touching `difficulty_profile.py` or the Speaker Difficulty Profile UI panel. |
| `REFORMULATION_RESEARCH.md` | Stage 5's deep research pass specifically on the *reformulation engine* architecture (as opposed to `RESEARCH.md`'s broader literature survey, or `PROBLEM_FORMULATION.md`'s profile-layer focus): minimal-edit tagging architectures (GECToR/FELIX), phoneme-position/stress granularity (Brown's four factors), student-hardware feasibility, a second close prior system (SpeechAgent, 2026), constructed failure modes, an explicit objective function, and a ranked architecture recommendation. | When the reformulation-engine design is revisited or the recommendation is acted on | New (2026-08-16). Research only — `grammar.py`/`semantic.py`/`engine.py`/`rewrite/`/`rephrase.py` are all unchanged; verify against `git log` before assuming otherwise. Read this (not `RESEARCH.md` alone) before designing the actual reformulation-engine implementation. |
| `REFORMULATION_PROBLEM_MAP.md` | **Living** Problem Definition / Research Map for the reformulation engine: nine semi-independent factors (intent inference from malformed input, in-context meaning preservation, grammaticality, naturalness/idiomaticity, profile-difficulty resolution, word sense, cross-substitution interaction, restructuring vs. substitution, and the change-amount trade-off), each checked against real code and pilot evidence, plus a dated research pass (idiom/MWE detection, WSD, constrained generation, GEC/clarification precedent, stuttering/AAC literature, controllable simplification, semantic-preservation metrics) with feasibility ratings and a proposed, unapproved implementation order. | **Every time** a pilot, eval run, or debugging session reveals a new factor, failure mode, constraint, or workable technique — unlike every other file in this table, not tied to a single dated stage. | New (2026-08-17). Read this before proposing or prioritizing any reformulation-engine fix — it is more current than `REFORMULATION_RESEARCH.md`'s architecture-selection framing, though it doesn't replace that document's own record. |
| `ARCHITECTURE_RESEARCH_R42_R43.md` | **Archival, not part of the standard reading chain.** Consolidates three previously-scattered standalone documents (R42's full architecture reassessment, R43's instrumented T5-escalation analysis, R43-A's bounded experiment results A1-A5) into one file, in original chronological order. Every actionable recommendation in it was later implemented and is documented with results in `VALIDATION.md` §35 onward — read `VALIDATION.md`/`DECISION_LOG.md` first for *what was decided*; come here only for the fuller original research argument behind those decisions. | Frozen — superseded content, not updated further | Consolidated 2026-08-26 from three files that existed since 2026-08-22/23 but were never added to this table or `CLAUDE.md`'s reading order — the exact clutter this row now fixes. |

## Live application path (the app depends on these; keep lean)

| File | Purpose | Notes |
|---|---|---|
| `app.py` | Streamlit UI (v8, rewritten 2026-08-16 around `reformulate.py`): one linear workflow — text entry → Speaker Difficulty Profile panel → Reformulate → changes/skipped/verification review with a per-change Keep/revert toggle. No login gate; loads one default profile automatically. | The old dual-pipeline UI (word-picker dropdowns, word/sentence/multi-sentence modes, profile-rewrite card, rephrase card, allowlist panel, the onset-risk chart) was removed, not kept for backward compatibility — see `DECISION_LOG.md` 2026-08-16-E for what and why. Still renders the "Speaker Difficulty Profile" panel (`_difficulty_profile()`, `_render_words_category()`, `_render_pattern_editor()`) unchanged. |
| `reformulate.py` | The consolidated reformulation engine (Architecture D′, `REFORMULATION_RESEARCH.md` §24–31), grown to three tiers as of R25: idiom guard (R19) → WSD-aware substitute-and-rank (R20, all-or-nothing per sentence) → phrase-level replacement for idiom-only cases (R25) → T5 restructuring escalation (generate-then-verify) → final re-verification → metrics, now including a second, independent MeaningBERT signal alongside SBERT (R24/R27). `app.py`'s only reformulation entry point. | Updated 2026-08-19 (R27) — was last updated 2026-08-16, went 5 roadmap items (R19-R27) stale before this refresh. Reuses `grammar.py`/`engine.py`/`semantic.py`/`phonetic.py`/`rephrase.py` as libraries — no duplicated candidate generation or scoring logic. `grammar.py::SentenceRewriter` and `rewrite/rewriter.py::DifficultyAwareRewriter` are not called from `app.py` but remain an actively-used comparison baseline in `eval/reformulation_eval.py`/`tests/roadmap_test.py`, not just "retained." Current, living design record: `REFORMULATION_PROBLEM_MAP.md`. |
| `naturalness.py` | "Naturalness of intervention" metric (ROADMAP.md R11): word-level `difflib.SequenceMatcher` edit-ratio and changed-word-count, no new dependency. | New (2026-08-16). Answers only "how much changed," not "was the change correct" — that's still `semantic.py`/`phonetic.py`'s job, reported as a separate metric per Practice.md §10. |
| `difficulty_profile.py` | `DifficultyProfile` — persistent, user-declared sounds/words/phrases, explicitly independent categories, plus (Stage 4A refinement) word-specific `problem_phones` patterns, `meta`-extensible entries. | New Stage 4A (2026-08-15), extended 2026-08-16. See `PROBLEM_FORMULATION.md` for full design rationale. Deliberately separate from `profiling/profile.py`'s learned `SpeakerDifficultyProfile` — reconciling them is `ROADMAP.md` R12, not yet done. |
| `profile_store.py` | File-based, single-default-profile storage (replaces `user_store.py`). Thin public API keyed by `profile_name` (default `"default"`) — kept parameterized, not hardcoded, so future multi-profile support stays possible without a data-model change. | New (2026-08-16). No password/auth concept — see `DECISION_LOG.md` 2026-08-16-A for why `auth.py`/`user_store.py` were deleted rather than kept as unused compatibility layers. Still writes into `users/` (not renamed to `profiles/`) because `profiling/profile.py` hardcodes that directory for its own files — see that module's row below. |
| `paths.py` | Must be imported **first**, before nltk/torch/wordfreq, in every entry module. Redirects caches into `./.cache/`, caps BLAS/OpenMP thread counts. | Load-bearing side-effect module; import-order is a real pitfall — see `HANDOFF.md`. |
| `grammar.py` | `sanitize_input()` (grammar correction, ~10 layers) + `SentenceRewriter` (the core substitution loop: POS-tag → protect → candidates → SBERT rank → phoneme gate → inflect → rebuild). Largest logic file (~1,600 lines). | Unmodified since Stage 4A and its refinement — verified via `tests/smoke.py` byte-identical output both times. |
| `engine.py` | `SynonymEngine` — WordNet (POS-filtered) + Datamuse candidate retrieval, Zipf-frequency ranking. | |
| `semantic.py` | SBERT loading, cosine similarity, protected-phrase/stop-word registry, combined semantic+frequency scoring. Since R19-R27: `IDIOM_PHRASES`/`IDIOM_PHRASE_PATTERNS`/`idiom_spans()` (the idiom guard, curated exact-match list, not a general MWE detector), `disambiguate_synset()` (WSD via SBERT-gloss matching), and `load_meaningbert()`/`meaningbert_score()` (the second, independently-trained meaning-preservation signal, reported alongside SBERT, never replacing it). | Owns the `MIN_SEMANTIC`/`SEMANTIC_W`/`FREQUENCY_W` constants that `README.md` describes inaccurately (see drift note above). `sem.MIN_SEMANTIC` is mutated at runtime from the UI slider — see `HANDOFF.md` pitfalls. |
| `phonetic.py` | ARPAbet onset extraction (CMU dict + grapheme fallback), the hard-coded `word_difficulty()` heuristic. | Standalone by design (imports only `paths`/nltk/wordfreq); the difficulty formula's weights are un-validated — see `DECISION_LOG.md` and `VALIDATION.md`. Stage 4A added `full_pronunciation()` (CMU-dict-only, informational, no scoring role, no OOV guessing); its refinement added `friendly_phone_label()` / `ARPABET_EXAMPLE_WORD` — a pronunciation-respelling table for showing phones to non-technical users, also display-only. |
| `freq.py` | Single choke-point for `wordfreq.zipf_frequency`, with an irreversible `MemoryError` downgrade to the small wordlist. | Global mutable state (`_active`) — see `HANDOFF.md` pitfalls. |
| `config.yaml` | Profiling/rewrite/eval knobs (`ewma_alpha`, profiling weights, `lambda`/`mu`/`tau`, detection thresholds). | These weights are the same class of un-validated, hand-picked numbers flagged by Practice.md §6/§7. The `profiling.detection` sub-block (repetition/prolongation/block/filler thresholds) is now only consumed by the archived `out_of_scope/profiling/detect.py` — left in place rather than pruned, since editing `config.yaml`'s structure was out of scope for a pure narrowing pass; flagged here as a loose end. |
| `rephrase.py` | T5 paraphrase layer — degrades to passthrough if weights are unavailable. Two callers as of R25: `reformulate.py`'s whole-sentence restructuring escalation (original role), and its phrase-level replacement tier (`generate_candidates()` reused unchanged, scoped to a local window instead of the whole sentence — no duplicated generation logic). Never imported by `grammar.py`/`engine.py`. | Cleanly isolated — good separation between live-path-required and optional-heavy code. Output is non-deterministic across process launches (beam search, no seed) — see `HANDOFF.md` pitfalls for the testing/diagnostic implications. |
| `profiling/profile.py` | `SpeakerDifficultyProfile` — persistent, multi-factor, EWMA-updated per-speaker difficulty model. | |
| `profiling/coldstart.py` | Population-prior + self-report blending for new profiles. | |
| `profiling/config.py` | Loads `config.yaml`. | |
| `rewrite/rewriter.py`, `rewrite/rank.py`, `rewrite/candidates.py` | The "soft" profile-aware rewrite path (`similarity - λ·difficulty + μ·frequency`), parallel to `SentenceRewriter` in `grammar.py`. | Meaningfully duplicates protected-word/POS-filtering/inflection logic already in `grammar.py`/`semantic.py` — flagged as an observation with maintenance-risk implications; see `DECISION_LOG.md` and `ROADMAP.md`. |
| `users/` | Per-profile JSON (`profile_name` + `difficulty_profile` only, as of the 2026-08-16 cleanup pass — no credentials, no preferences; see `profile_store.py`). Directory name predates and outlives the removed auth system — kept because `profiling/profile.py` hardcodes it for `*.fluency_profile.json`, unrelated to this. | `default.json` is still committed to the repo (by design now — it's the single default profile's data, not a credential). `bobcat.json` (a second test account, SHA-256 password hash included) was deleted 2026-08-16, `git rm` — recoverable from history, not from the working tree. The **old commits** containing both files' password hashes are unaffected — see `ROADMAP.md` R0's updated status. The pre-`reformulate.py` UI's `custom_replacements`/`preferences` fields (allowlist, rephrase/profile-rewrite toggles) are dropped from the file the next time it's saved — they had zero live consumers once the UI controls that set them were removed. |
| `out_of_scope/voice.py`, `out_of_scope/profiling/asr.py`, `out_of_scope/profiling/detect.py` | **Moved out of the live app, this pass.** Browser STT/TTS UI, the CrisperWhisper ASR wrapper, and the rule-based ASR-timing disfluency detector. Audio Module concerns, not text reformulation. | See `out_of_scope/README.md`. Not imported by `app.py`/`profiling/__init__.py` anymore; preserved standalone with a duplicated `config.py` so they still run/test in isolation. |

## Research / evaluation path (should not be imported by the live app)

| File | Purpose | Notes |
|---|---|---|
| `eval/metrics.py` | Meaning-preservation, difficulty-onset reduction, substitution-rate, λ-tradeoff computations. | Computed entirely from SBERT/frequency/formula signals — see the proxy-metric caveat in `VALIDATION.md`. |
| `eval/profile_eval.py` | Compares self-report-only vs. observed-only vs. fused profile AUC. | |
| `eval/study/collect.py`, `counterbalance.py`, `stats.py` | Scaffolding for a counterbalanced three-condition user study. | Not yet run against real participants, as far as this review can tell from the repo alone (observation, not confirmed either way — see `VALIDATION.md`). |
| `scripts/build_rephrase_dataset.py`, `scripts/train_rephrase.py` | Offline dataset-building / fine-tuning scaffolding for the T5 rephrase model. | |
| `tests/smoke.py` | Behavioral baseline/regression diff tool (not a real evaluation — a regression net, per its own docstring). | Requires `DISABLE_DATAMUSE=1` for determinism. |
| `tests/app_test.py` | Headless Streamlit `AppTest` UI smoke test. No longer seeds a login/auth session (there's no login) — scenarios write a known profile state directly to `users/default.json` via `DifficultyProfile`/`profile_store` before each run, letting the app's real startup path load it. 6 scenarios as of the Stage 4A refinement, including one that drives the word-specific pattern-editor checkboxes by widget key. | |
| `tests/threshold_sweep.py` | Diagnostic sweep of `MIN_SEMANTIC`. **Already produced a documented, unactioned finding** — see `DECISION_LOG.md` 2026-06-08-A and `VALIDATION.md`. |
| `tests/evaluate.py`, `tests/roadmap_test.py` | Additional regression/eval scripts. | `roadmap_test.py` was split in Stage 2: its `SpeakerDifficultyProfile`/`DifficultyAwareRewriter` tests stayed here; its CrisperWhisper/detector tests moved to `out_of_scope/tests/roadmap_test_audio.py` alongside the code they exercise. Re-ran both halves after the split — all pass, no behavior change. |
| `tests/difficulty_profile_test.py` | 38 tests for `difficulty_profile.py` — add/remove/dedup per category, text edge cases, pronunciation derivation (including OOV), persistence across a simulated restart, legacy-profile migration, and (12 new, 2026-08-16) word-specific `problem_phones`: set/clear, validated against the word's real pronunciation, that setting one never creates a global sound entry, explicit promotion and its dedup behavior, persistence. | New Stage 4A (2026-08-15), extended 2026-08-16. `tests/app_test.py` also exercises the same features through the actual Streamlit widgets (scenarios 5–6). |
| `tests/baseline*.txt`, `tests/eval_corpus.txt`, `tests/eval_results.csv` | Committed reference outputs for the regression-diff tests. | These are regression fixtures, not pre-registered (§8) evaluation results. `tests/smoke.py` output is byte-identical to `baseline_sbert.txt` after this pass's `app.py` edits, confirming the rewrite pipeline itself was untouched. `baseline.txt`/`baseline_sbert.txt` were regenerated 2026-08-17 for the idiom guard (R19) — the one diff in each (a "right now" sentence no longer wrongly substituted) is the intended behavior change, verified isolated before committing. |
| `tests/semantic_test.py` | Unit tests for `semantic.py`'s `protected_positions()`/`idiom_protected_positions()`/`idiom_spans()` — the idiom/fixed-expression guard (R19, extended R25/R27) and WSD. No model loading required (pure token matching). 46 tests as of R27. | Updated 2026-08-19 (R27) — was "New (2026-08-17)". |
| `eval/idiom_guard_recheck.py` | Read-only diagnostic: re-runs the frozen v3 pilot corpus (`eval/pilot_pairs.json`) through the current engine and diffs against what P1 actually rated. Originally built for R19's verification only — **since reused for R20, R25, R26, and R27**, each time paired with `git stash` to isolate that specific change's true effect from the corpus's accumulated drift since the original v3 capture. Never writes to `eval/pilot_pairs.json`/`eval/pilot_responses/`. | Updated 2026-08-19 (R27) — this is now the established, reusable pattern for verifying any engine change against the frozen corpus, not a one-off tool as previously described. Its `TARGET_PAIR_IDS` set needs updating by hand each time a fix's target pairs change — see its own comments for the current list and why. |
