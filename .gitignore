# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.pyo
*.pyd
.Python

# ── Virtual environments ──────────────────────────────────────────────────────
.venv/
.venv*/
venv/
env/
ENV/
.env/
.vendor/

# ── Distribution / packaging ──────────────────────────────────────────────────
dist/
build/
*.egg-info/
*.egg
.eggs/

# ── NLTK data (downloaded at runtime, not committed) ─────────────────────────
nltk_data/

# ── SBERT / sentence-transformers + all runtime model/data caches ─────────────
# The SBERT model, torch hub, and NLTK data are downloaded to .cache/ on first
# run.  Do NOT commit — too large and regenerates automatically.
.cache/

# ── Phase 9/9B validator prototype — fine-tuned weights, not source,
# reproducible from eval/r9_train_validator.py / r9b_train_validator.py.
# Do NOT commit.
eval/r9_validator_model/
eval/r9b_validator_model/
eval/r9b_sanity_checkpoints/
eval/r9c_validator_model/

# ── User data written at runtime ──────────────────────────────────────────────
users/*.fluency_profile.json

# ── Streamlit ─────────────────────────────────────────────────────────────────
.streamlit/secrets.toml

# ── Jupyter / IPython ─────────────────────────────────────────────────────────
.ipynb_checkpoints/
*.ipynb

# ── IDEs ──────────────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~

# ── OS ────────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Logs & temp ───────────────────────────────────────────────────────────────
*.log
*.tmp
*.bak
/tmp/

# ── Test artefacts ────────────────────────────────────────────────────────────
.pytest_cache/
.coverage
htmlcov/

# ── Stage LR: real-participant data (path (b)) — local only by user decision, ──
# 2026-08-30 — never commit real friends' declared difficulty profiles,
# their sentences, or their judgments, even unnamed. The MECHANISM
# (stage_lr/harvest_real_profile.py, stage_lr/ingest_real_human_pair.py)
# is generic and stays tracked; only actual per-participant content,
# entirely confined to this directory, is excluded.
stage_lr/data/private/
stage_lr/data/real_human_pairs.json
